{
  "name": "predict",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "install": "pip install -r requirements.txt",
    "dev": "python -m flask run",
    "start": "python -m flask run"
  }
}
